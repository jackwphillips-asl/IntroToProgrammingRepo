var fontsize = 20;
var maxsize = 30;
var minsize = 8;

// Makes the font size bigger, turns red if too big (or small), and changes the message to display the font size.
// --> void
function makeBigger() {

}

// Makes the font size bigger, turns red if too big (or small), and changes the message to display the font size.
// --> void
function makeSmaller() {

}



