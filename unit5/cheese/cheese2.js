/* MOD 2: Create the same picture as the base */
var myGreen;
var myX, myY;

function setup() {
    createCanvas(500, 500);
    myGreen = color(0, 200, 100);
	myX = 75;
	myY = 100;
}

function draw() {  
    drawCheese();
}

function drawCheese() {
    // TODO Add cheese code here from base
    // so that picture looks like base
}